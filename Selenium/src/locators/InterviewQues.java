package locators;

public class InterviewQues {

	public static void main(String[] args) {

		//write xpath for all the limks = (//a)
		//write xpath for all the 5th links = (//a)[6]
		//write xpath for all the last link = (//a)[last()-1]
		//write xpath for all the last links = (//a)[last()]
		//image=<img> tag
		//link=<a> tag
		//text=<span> tag

	}

}
